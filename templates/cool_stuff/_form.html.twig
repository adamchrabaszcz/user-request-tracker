{{ form_start(form) }}
    {{ form_widget(form, { 'attr': {'class': 'form-inline'} }) }}
    <br>
    <button class="btn btn-outline-primary btn-sm">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}